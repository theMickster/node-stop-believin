/**
 * Media content related types for entity composition
 * NOT FOR EXPORT - Internal to entities folder only
 */

interface MediaContent {
  url?: string;
  thumbnailUrl?: string;
  imageUrl?: string;
}

export type { MediaContent };
