/**
 * Social media related types for entity composition
 * NOT FOR EXPORT - Internal to entities folder only
 */

interface SocialMediaLinks {
  twitter?: string;
  instagram?: string;
  facebook?: string;
  linkedin?: string;
  goodreads?: string;
  amazonAuthor?: string;
}

export type { SocialMediaLinks };
